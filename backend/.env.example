# Backend environment variables
# On Vercel, set these in Project Settings → Environment Variables

PORT=5000
NODE_ENV=development

# MongoDB connection (use MongoDB Atlas in production)
MONGODB_URI=mongodb://localhost:27017/volunteer-hub

# CORS origin for local development
FRONTEND_URL=http://localhost:5173

# Civic Auth credentials (server-side if needed)
CIVIC_AUTH_CLIENT_ID=
CIVIC_AUTH_CLIENT_SECRET=

# JWT secret (if switching to JWT-based sessions later)
JWT_SECRET=change_me